#[test]
fn test_for_char() {
    // let mut s = String::from("Hello world");
    // unsafe {
    //     for ch in s.as_bytes_mut().iter_mut() {
    //         ch = b'c';
    //         println!("{}", ch);
    //     }
    // }

    let mut char_var = [b'a', b'a', b'a', b'a', b'a', b'a'];
    char_var[0] = b'x';
    // println!("{}", String::asc);

    // let str_ite = s.chars();
    // for mut i in str_b {
    //     i = &（b'A'）;
    // }
    // let a = s.chars().nth(2).unwrap();
}
